<?xml version="1.0" encoding="utf-8"?>
<!--
  Public-release permission hardening.

  The base manifest historically requested broad storage and battery-optimization
  special access. Those are not required for the core camera/recording experience
  on modern Android and create unnecessary Play policy/review surface.

  Release builds use MediaStore/SAF/app-private storage instead.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.WRITE_MEDIA_VIDEO"
        tools:node="remove" />

    <application
        android:requestLegacyExternalStorage="false"
        tools:replace="android:requestLegacyExternalStorage" />
</manifest>
