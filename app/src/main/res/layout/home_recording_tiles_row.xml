<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="6dp"
    android:paddingBottom="6dp"
    android:weightSum="4">

    <!-- AF mode toggle (continuous / off) -->
    <TextView
        android:id="@+id/tile_af_toggle"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_weight="1"
        android:layout_marginEnd="4dp"
        android:background="?attr/colorDialog"
        android:gravity="center"
        android:fontFamily="@font/materialicons"
        android:text="center_focus_strong"
        android:contentDescription="Autofocus toggle"
        android:textColor="?attr/colorOnSurface"
        android:textSize="18sp"
        android:padding="6dp"
        android:clickable="true"
        android:focusable="true" />

    <!-- AE lock toggle -->
    <TextView
        android:id="@+id/tile_ae_lock"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_weight="1"
        android:layout_marginEnd="4dp"
        android:background="?attr/colorDialog"
        android:gravity="center"
        android:fontFamily="@font/materialicons"
        android:text="lock_open"
        android:contentDescription="AE lock toggle"
        android:textColor="?attr/colorOnSurface"
        android:textSize="18sp"
        android:padding="6dp"
        android:clickable="true"
        android:focusable="true" />

    <!-- Exposure (single tile opens slider picker) -->
    <TextView
        android:id="@+id/tile_exp"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_weight="1"
        android:layout_marginEnd="4dp"
        android:background="?attr/colorDialog"
        android:gravity="center"
        android:fontFamily="@font/materialicons"
        android:text="exposure_plus_1"
        android:contentDescription="Exposure compensation"
        android:textColor="?attr/colorOnSurface"
        android:textSize="18sp"
        android:padding="6dp"
        android:clickable="true"
        android:focusable="true" />

    <!-- Tap-to-focus (instructional) -->
    <TextView
        android:id="@+id/tile_tap_focus"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_weight="1"
        android:background="?attr/colorDialog"
        android:gravity="center"
        android:fontFamily="@font/materialicons"
        android:text="info"
        android:contentDescription="Tap-to-focus help: Tap preview to focus"
        android:textColor="?attr/colorOnSurface"
        android:textSize="18sp"
        android:padding="6dp"
        android:clickable="true"
        android:focusable="true" />

</LinearLayout>
