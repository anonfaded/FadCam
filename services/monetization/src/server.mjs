import {serve} from '@hono/node-server'; import {Hono} from 'hono'; const app=new Hono(); const port=Number(process.env.PORT||8085); app.get('/health',c=>c.json({service:'fad-monetization',status:'ok'})); app.get('/api/v1/offers',c=>c.json({items:[]})); serve({fetch:app.fetch,port});