{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "FadCam: build and install debug",
            "type": "shell",
            "command": "./build.sh debug true"
        },
        {
            "label": "FadCam: build and install release",
            "type": "shell",
            "command": "./build.sh release true"
        },
        {
            "label": "FadCam: build only debug",
            "type": "shell",
            "command": "./build.sh debug false"
        },
        {
            "label": "FadCam: build only release",
            "type": "shell",
            "command": "./build.sh release false"
        }
    ]
}